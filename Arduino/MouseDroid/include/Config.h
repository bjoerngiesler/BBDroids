#if !defined(CONFIG_H)
#define CONFIG_H

#include <sys/types.h>
#include <Arduino.h>

static const uint8_t RX_PIN = D7;
static const uint8_t TX_PIN = D6;
static const uint8_t PULSEIN_PIN = D0;

#endif // CONFIG_H